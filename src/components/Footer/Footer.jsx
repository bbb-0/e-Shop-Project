const Footer = () => {
	return (
		<div className={styles.footer}>
			<footer className={styles.footer_content}>
				{/* Link to socials / github repo / linkedin */}
				<div></div>
			</footer>
		</div>
	);
};

export default Footer;
